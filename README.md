# P&ID → Graph Conversion & SOP Cross-Referencing

Converts a P&ID diagram into a structured, attributed graph and cross-references it
against an SOP document to flag discrepancies. Built and validated against a
synthetic fixture first, then run against the real Interface assignment files —
see "Tested Against Real Data" below for what changed once real documents were in
the loop.

## Install

```bash
pip install -r requirements.txt
```

Tesseract OCR must also be installed as a system binary (pytesseract is just a wrapper
around it):

```bash
sudo apt-get install tesseract-ocr   # Debian/Ubuntu
brew install tesseract                # macOS
```

## Run

```bash
python main.py --pid data/pid/diagram.pdf --sop data/sop/sop.docx --out output/
```

`data/pid/diagram.pdf` and `data/sop/sop.docx` are the real Interface assignment
files (a 3-sheet P&ID set and its design-limits SOP — see below). Multi-sheet PDFs
are processed page by page and merged automatically.

Outputs written to `output/`:

- `pid_graph.json` / `pid_graph.graphml` — the extracted component graph (NetworkX)
- `cross_reference_report.json` / `.log` — discrepancies found, ranked by severity

## Test

```bash
python -m pytest tests/ -v
```

33 tests, happy/edge/failure path per module — all against `tests/fixtures/`, a
small synthetic P&ID + SOP generated by `scripts/generate_sample_data.py`, kept
separate from the real assignment data so the suite is deterministic and fast
(no OCR-on-real-CAD-art flakiness, no dependency on files that might get replaced).
The integration test asserts every discrepancy category the synthetic fixture was
designed to trigger actually fires, and that the one SOP-stated connection matching
a real P&ID edge is correctly *not* flagged.

Regenerate the synthetic fixtures with `python scripts/generate_sample_data.py`.
The scene: a 4-component chain (`T-101` tank → `P-101` pump → `V-101` valve →
`FT-101` instrument) plus deliberate traps — an isolated valve never mentioned in
the SOP, an unlabeled symbol, an SOP reference to a tag that doesn't exist in the
diagram, a mislabeled component type, and a stated connection that doesn't exist.
See that script's module docstring for the full list.

## Approach

```
PDF → image per sheet (PyMuPDF)
    → shape detection (OpenCV contours: rectangle=tank, circle=instrument,
      circle+child-triangle=pump, notched/concave polygon=valve)
    → OCR tag extraction per shape (Tesseract, cropped label region below/around
      each symbol, regex-normalized against ISA tag format e.g. "FT-101")
    → connector detection (mask out symbol interiors, Hough line detection on
      what's left, attach each segment's endpoints to the nearest symbol)
    → NetworkX graph, merged across sheets

SOP .docx → python-docx
    → narrative text → tag / declared-type / stated-connection extraction (regex)
    → tables → per-tag design pressure & temperature limits

P&ID sheet header/title block → OCR → per-tag design pressure & temperature limits

Two independent cross-reference passes, findings merged into one severity-ranked report:
  1. graph vs. narrative SOP facts     (MISSING_IN_PID, CONNECTION_MISMATCH, ...)
  2. P&ID title-block limits vs. SOP limits table  (DESIGN_LIMIT_MISMATCH)
```

No labeled P&ID symbol dataset is available for this assignment, so training or
fine-tuning a detector (YOLO et al.) wasn't realistic in the time available.
Classical CV (OpenCV contours + Tesseract OCR) is deterministic, needs no training
data, and is transparent about *why* it classified something a given way — but it
assumes reasonably clean, stylized diagrams. `shape_detection.detect_shapes()` is
the only place a trained detector would plug in; everything downstream consumes its
`DetectedShape` output and is agnostic to how shapes were found.

**Tag typing:** a component's type is read from its ISA tag prefix when OCR
succeeds (e.g. `FT-` → flow transmitter, `V-` → valve — see
`src/component_types.py`), falling back to the shape classifier only when no tag
was read. Tag prefix is authoritative because it's the real engineering data.

**Discrepancy categories:**

| Category | Severity | Meaning |
|---|---|---|
| `MISSING_IN_PID` | ERROR | SOP references a tag not found in the diagram |
| `CONNECTION_MISMATCH` | ERROR | SOP states two components connect; no such edge was detected |
| `DESIGN_LIMIT_MISMATCH` (pressure) | ERROR | P&ID and SOP disagree on a tag's design pressure |
| `TYPE_MISMATCH` | WARNING | SOP describes a component's type differently than the diagram classified it |
| `UNRESOLVED_TAG` | WARNING | A symbol was detected but OCR couldn't read its tag |
| `DESIGN_LIMIT_MISMATCH` (temperature) | WARNING | Same, for temperature — lower severity because temperature OCR proved noisier than pressure on the real drawing |
| `MISSING_IN_SOP` | INFO | A diagram component is never mentioned in the SOP |

## Tested Against Real Data

The synthetic fixture above was built *before* the real assignment files arrived,
specifically so the pipeline had something deterministic to prove itself against.
Once the real `diagram.pdf` (a 3-sheet industrial P&ID — Consol Energy Marcellus
Shale gas processing, particulate filters/NGL stabilizer/heat exchangers) and
`sop.docx` showed up, I ran the pipeline against them and adapted based on what I
found. Both parts of that story are below.

### The SOP wasn't what I assumed

The synthetic fixture's SOP extraction is built for narrative text ("open valve
V-101..."). The real SOP turned out to be a **design/operating limits table**
(equipment name → pressure → temperature), not a procedure. Rather than force-fit
the narrative extractor, I added a second, table-aware extraction path
(`sop_extraction/limits_table.py`) and a matching cross-reference check
(`crossref/limit_check.py`) — the narrative path stays in place for SOPs that do
look like procedures; both run and their findings merge. One real wrinkle: `E-742`
has two rows in the table (shell-side and tube-side limits differ but share a tag)
— `extract_equipment_limits` returns a list per tag, not a single value, to keep
both.

### Title-block cross-check: this is the part that actually works well on real data

Each P&ID sheet has a header note stating that sheet's equipment and its design
limits, e.g. `F-715 A & B ... DESIGN: 275 PSIG @ 100F` — the same numbers the SOP
table has. Extracting and comparing these (`pid_extraction/title_block.py`) is the
cross-check that's actually meaningful on this specific document pair, closer to
what a compliance tool like Interface would do than the synthetic scene's graph
story. Two problems came up and both are handled, not papered over:

1. **OCR reliably mangles the tag's digits.** The tag line sits directly above an
   underline rule; `F-715` consistently read as `F-/I39` or similar, even at 600
   DPI and after erasing the underline stroke — but the *letters* always came
   through clean. Since a tag prefix is unique within this document (only one `F-`
   tag, one `V-` tag, ...), matching on prefix alone against the known-tag
   vocabulary from the SOP table is more robust than trying to reconstruct exact
   digits from noisy OCR. Getting this right also meant *not* over-matching: a
   naive "prefix + any character" match collides with `°F`, the temperature unit,
   so the separator character specifically has to look dash-like.
2. **Sheets can name two equipment items side by side** (page 2: `AC-746` and
   `E-742`), each with its own limits clause. A single text-wide search would
   misattribute one clause's numbers to both tags — fixed by pairing tags and
   limit clauses by left-to-right position in the OCR'd text instead of a global
   search.

**Result on the real document pair: all 4 equipment tags with a design limit in
the SOP table were found and matched in the P&ID title blocks, 0 mismatches** —
pressure matched exactly on every tag; temperature matched after normalizing OCR
punctuation noise (`"3/75"` → digits `375`, equal to the SOP's `375`). That's a
real, verified-consistent finding, not a demo. `test_limit_check.py` and
`test_title_block.py` cover the mismatch-detection and OCR-tolerance logic in
isolation, since the real data itself happens to be fully consistent and so
doesn't exercise the ERROR path.

### Component-graph extraction: honest results, and why

Running the raster shape/connector pipeline (built and tuned against the
synthetic fixture) against the real sheets extracted **1 component, 0 connections**
per sheet — a real result, not a bug I didn't notice. Two things about real,
CAD-exported P&IDs broke the assumptions the synthetic fixture didn't test:

1. **ISA symbols are drawn as thin outlines, not solid fills.** The morphological
   opening step that separates thin connector lines from thick symbol bodies
   (necessary — see below) erases the symbols too, because in a real drawing
   symbols and pipes share the same line weight. There's no thickness signal left
   to tell them apart.
2. **The entire sheet is one connected shape**, topologically. Border, grid-number
   ladder, pipe network, and every symbol touch each other somewhere, so contour
   tracing on the raw raster finds essentially one giant blob (I measured this
   directly: 49 top-level contours on a full sheet, one of them the border at 88%
   of the page area, the rest under 300px² — nothing symbol-sized survives).

I investigated whether the PDF has real vector path data as an alternative to
raster contour-tracing — it does (5,368 vector drawing objects on page 0 alone,
each with exact geometry, not rasterized). That would sidestep both problems above
in principle. In practice, this document's text is *also* rendered as vector paths
(no extractable text layer — `page.get_text()` returns almost nothing), so
distinguishing a true instrument-bubble circle from a vectorized digit glyph from a
junction dot requires the same kind of shape discrimination as the raster approach,
just on path primitives instead of pixels — not a shortcut, a different multi-day
problem (this is genuinely what dedicated P&ID digitization products exist to
solve). I scoped it out rather than ship a half-finished second pipeline; a
production version of this tool would need either a symbol-trained detector or a
proper vector-path symbol classifier, not a heuristic extension of what's here.

**What this means concretely:** the graph/cross-reference machinery (tested,
validated, and demonstrably correct against the synthetic fixture's 6-component
scene) needs a better symbol source to produce a useful component graph on *this
specific class* of real drawing. The design-limits cross-check above doesn't
depend on that symbol source at all, which is exactly why it's the part that
delivers a real, verified result today.

## Limitations (honest, not hidden)

- Connector detection assumes straight or near-straight pipe runs and solid-fill
  symbols — see "Component-graph extraction" above for what that costs on a real
  CAD-exported P&ID specifically.
- OCR tag matching is regex-anchored to ISA-style tags (`[A-Z]{1,4}-\d{2,4}[A-Z]?`
  in the diagram body; a looser prefix-vocabulary match in title blocks). A
  different tagging convention would need `src/component_types.py::TAG_CORE`
  updated.
- SOP narrative relation extraction (`"X connects to Y"`) is phrase-pattern
  matching, not full NLP. Given more time, this is the piece I'd hand to an
  LLM-assisted extraction pass instead of regex.
- Title-block temperature parsing is lower-confidence than pressure (see
  `DESIGN_LIMIT_MISMATCH` severity split) — OCR corrupts punctuation inside the
  temperature clause more than the pressure clause on this drawing.

## Bonus not attempted (time-boxed)

Web UI visualization and DB-backed storage were left out to prioritize a correct,
tested, honestly-scoped core pipeline — including actually running it against the
real files and adapting, rather than a demo that only works on a toy scene. The
NetworkX graph already exports to JSON/GraphML, so a visualization layer (e.g.
pyvis/D3 reading `pid_graph.json`) is a bounded follow-on, not a rearchitecture.
